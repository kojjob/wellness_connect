# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

availability_one:
  provider_profile: provider_profile_one
  start_time: <%= (Time.current.utc.hour >= 8 ? 3.days.from_now : 2.days.from_now).beginning_of_day + 9.hours %>
  end_time: <%= (Time.current.utc.hour >= 8 ? 3.days.from_now : 2.days.from_now).beginning_of_day + 10.hours %>
  is_booked: false
